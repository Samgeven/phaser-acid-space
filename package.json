{
  "name": "phaser-acid-space",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "prettier-format": "prettier --config .prettierrc.cjs 'src/**/*.ts' --write",
    "deploy": "gh-pages -d public"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "prettier": "^3.0.3",
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  },
  "dependencies": {
    "phaser": "^3.55.2",
    "vitest": "^0.34.3"
  }
}
